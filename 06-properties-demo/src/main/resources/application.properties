#configure port server
server.port = 8585

#configure my props
coach.name= RIZKI EKA ramadhan
team.name=KuroOwn

# set  the context path of the application
server.servlet.context-path=/mycoolinfo

# Use wildcard "*" to expose all endpoints
# can also expose individual endpoints with a comma-delimited list
#
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# Exclude individual enpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=info,health

info.env.name=MySpringApp
info.env.description=a crazy app
info.env.version=1.0.0